resources:
  - ./db/postgres-deployment.yaml
  - ./db/postgres-pvc.yaml
  - ./db/postgres-svc.yaml


configMapGenerator:
  - name: fastapi-config
    envs:
      - fastapi/fastapi.env
  - name: vue-config
    envs:
      - vue/vue.env

generatorOptions:
  disableNameSuffixHash: true
